version: "3.9"

services:

  ai-service:
    build:
      context: ../services/ai-service

    container_name: botuncle-ai

    ports:
      - "8000:8000"

    env_file:
      - .env

    depends_on:
      - redis

    volumes:
      - ../services/ai-service:/app

    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  redis:
    image: redis:7-alpine
    container_name: botuncle-redis
    ports:
      - "6379:6379"